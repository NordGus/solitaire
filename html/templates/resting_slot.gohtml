{{ define "resting_slot" }}
    <game-resting-slot
            class="relative w-[7rem] after:block after:w-[7rem] after:h-[12.5rem] after:rounded after:border-[2px] after:border-amber-600 after:bg-red-950"
            data-family="{{ .Family }}"
            data-direction="{{ .Direction }}"
            data-attachable="{{ .Attachable }}"
    ></game-resting-slot>
{{ end }}
